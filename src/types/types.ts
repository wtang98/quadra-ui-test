export type initialStateType = {
    number: number;
};

export type mainPageHooksReturnType = {
    number: number;
    numberColor: colorDictonary;
};

export enum colorDictonary {
    red = "text-red-500",
    white = "text-white",
    green = "text-green-500",
}
